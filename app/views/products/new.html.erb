<%- model_class = Product -%>
  <% if current_user.status == 'Ativo' %>
    <div class="page-header">
      <h1><%=t '.title', :default => [:'helpers.titles.new', 'Novo %{model}'], :model => model_class.model_name.human.titleize %></h1>
    </div>
  <% else %>
    <div class="page-header">
      <h1> Usuário <%= current_user.status %> </h1>
    </div>
  <% end %>  

<%= render :partial => 'form' %>
